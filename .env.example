X402_ENABLED=false
X402_ENVIRONMENT=development
X402_PRICE=$0.01

# Required by Coinbase CDP when x402 is enabled
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
CDP_WALLET_SECRET=

# Comma-separated lower/upper-case EVM addresses are normalized at runtime
SANCTIONED_ADDRESSES=
